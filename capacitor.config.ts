import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demoapp.instaclone',
  appName: 'instaClone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
